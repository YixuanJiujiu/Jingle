"""
    Setup
"""

from setuptools import setup

setup(name='Jinglescheduler',
      version='0.1',
      description='Jingle project Kubernetes Driver',
      author='YW',
      packages=['scheduler','worker'],
      zip_safe=False)
